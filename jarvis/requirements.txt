# ============================================================
#  JARVIS - CORE requirements
#  Install:  pip install -r requirements.txt
# ============================================================

# --- HTTP / APIs -------------------------------------------------
requests>=2.31.0,<3.0.0

# --- WEB APP -----------------------------------------------------
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.6.0,<3.0.0
httpx>=0.27.0,<1.0.0

# --- VOICE: sun'na (Speech -> Text) -------------------------------
SpeechRecognition>=3.10.0,<4.0.0
sounddevice>=0.5.0,<1.0.0
numpy>=1.26.0,<3.0.0

# --- VOICE: bolna (Text -> Speech) -------------------------------
gTTS>=2.5.0,<3.0.0
playsound==1.2.2
pygame>=2.5.0,<3.0.0
edge-tts>=6.1.11,<7.0.0

# --- System info -------------------------------------------------
psutil>=5.9.0,<7.0.0
